# 📂 完整專案結構樹狀圖

```
未命名檔案夾/
│
├── 📁 api/                          # Vercel Serverless 函數
│   ├── health.js                   # ✅ 健康檢查 API
│   ├── chat.js                     # 💬 AI 聊天 API
│   └── contact.js                  # 📧 聯絡表單 API
│
├── 📁 backend/                     # 後端代碼（本地開發用）
│   ├── server.js                   # 🚀 伺服器入口（啟動檔案）
│   ├── app.js                      # ⚙️  Express 應用配置
│   │
│   ├── 📁 routes/                  # API 路由（功能模組）
│   │   ├── health.js              # 健康檢查路由
│   │   ├── chat.js                # AI 聊天路由
│   │   └── contact.js             # 聯絡表單路由
│   │
│   ├── 📁 middleware/              # 中介層
│   │   ├── errorHandler.js        # 全局錯誤處理
│   │   └── corsMiddleware.js      # CORS 配置
│   │
│   └── 📁 utils/                   # 工具函數
│       └── aiResponse.js           # AI 回應生成模塊
│
├── 📁 public/                      # 前端靜態文件
│   ├── index.html                  # 🏠 主頁面
│   ├── styles.css                  # 🎨 玻璃擬態設計樣式
│   └── script.js                   # ⚡ 互動邏輯 & API 調用
│
├── 📁 node_modules/               # 依賴包（git 忽略）
│
├── 📄 package.json                 # 📦 項目依賴配置
├── 📄 package-lock.json            # 📌 依賴鎖定版本
├── 📄 vercel.json                  # ☁️  Vercel 部署配置
├── 📄 .env                         # 🔐 環境變數（本地）
├── 📄 .env.example                 # 📋 環境變數範例
├── 📄 .gitignore                   # 🚫 Git 忽略規則
├── 📄 README.md                    # 📖 項目文檔
└── 📄 SETUP.md                     # 🛠️  設置指南

```

## 📊 各資料夾用途說明

### 🌐 `/api` - Vercel Serverless 函數
- **用途**: 部署到 Vercel 時的無伺服器函數
- **包含**: 三個主要 API 端點
- **優點**: 無需管理伺服器，自動擴展

### 🔧 `/backend` - 後端應用
- **用途**: 本地開發時的完整 Express 應用
- **結構分類**:
  - `routes/` - 按功能模組組織 API 路由
  - `middleware/` - 中介層處理
  - `utils/` - 可重用工具函數

### 🎨 `/public` - 前端資源
- **用途**: 靜態文件託管
- **包含**: HTML、CSS、JavaScript
- **效果**: 玻璃擬態設計 + 溫暖色調

## 📝 主要文件功能

| 文件 | 功能 | 技術 |
|------|------|------|
| `backend/server.js` | 伺服器啟動 | Node.js |
| `backend/app.js` | 應用配置 | Express |
| `backend/routes/*.js` | API 路由 | Express Router |
| `backend/middleware/*.js` | 中介層 | Express Middleware |
| `backend/utils/aiResponse.js` | AI 邏輯 | 關鍵字匹配 |
| `public/index.html` | 頁面結構 | HTML5 |
| `public/styles.css` | 視覺設計 | CSS3 Glassmorphism |
| `public/script.js` | 交互邏輯 | Vanilla JS |
| `api/*.js` | Serverless 函數 | Vercel Functions |
| `.env` | 環境配置 | 配置文件 |

## 🔄 數據流向

```
用戶端                後端                AI 引擎
  │                   │                   │
  ├─→ HTML/CSS/JS ────→ 靜態文件託管 ◄────┘
  │
  ├─→ POST /api/chat
  │        ↓
  ├─────→ 驗證消息
  │        ↓
  ├─────→ generateAIResponse()
  │        ↓
  ├─────→ 返回 AI 回應
  │        ↓
  └─←──── 更新聊天框
```

## 🚀 部署架構

### 本地開發
```
npm run dev
    ↓
backend/server.js 啟動
    ↓
Express 監聽 3000 端口
    ↓
提供靜態文件 + API
```

### Vercel 部署
```
Git Push
    ↓
Vercel 檢測 vercel.json
    ↓
自動部署 /api 函數
    ↓
靜態文件託管
    ↓
CDN 加速
```

## 🎯 特點總結

✅ **前後端分離** - 清晰的目錄結構
✅ **模組化設計** - 易於維護和擴展
✅ **響應式設計** - 支持所有設備
✅ **無框架依賴** - 前端使用原生 JS
✅ **Serverless** - 支持 Vercel 部署
✅ **生產就緒** - 完整的錯誤處理和驗證
✅ **文檔齊全** - README + SETUP 指南

---

現在你的專案已準備好開發！請參考 `SETUP.md` 進行初始化。
